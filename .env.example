# AutoSEO Dashboard Environment Variables

# API base URL — set to your autoseo backend URL
# Leave empty to use relative URLs (same-origin proxy)
VITE_API_URL=

# Set to "true" to bypass API calls and use sample data directly
# Useful for local development without a running backend
VITE_USE_SAMPLE=true
